{
  "name": "tecnopos",
  "version": "1.0.1",
  "private": true,
  "description": "TecnoPOS — plataforma multi-sede para cadenas de restaurantes",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "build": "npm install --prefix server --omit=dev && npm install --prefix client && npm run build --prefix client",
    "start": "node server/src/index.js",
    "seed": "node server/src/seed/run.js",
    "postinstall": "npm run build"
  },
  "engines": {
    "node": ">=18 <23"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.14.0",
    "morgan": "^1.10.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
